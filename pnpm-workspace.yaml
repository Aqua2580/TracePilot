packages:
  - "apps/*"
  - "packages/*"

# Node 版本约束：better-sqlite3@12 通过 prebuild-install 提供 Node 22 与 24
# 的预编译二进制；engines.node 声明 >=22.0.0，engineStrict 确保下限检查。
engineStrict: true

allowBuilds:
  esbuild: true
  better-sqlite3: true

onlyBuiltDependencies:
  - esbuild
  - better-sqlite3
